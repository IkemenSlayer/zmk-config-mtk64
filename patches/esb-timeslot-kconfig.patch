--- a/src/split/Kconfig
+++ b/src/split/Kconfig
@@ -9,7 +9,7 @@
     select ESB_DYNAMIC_INTERRUPTS
     select DYNAMIC_INTERRUPTS
     select DYNAMIC_DIRECT_INTERRUPTS
-    select MPSL_DYNAMIC_INTERRUPTS if !BLE
+    select MPSL_DYNAMIC_INTERRUPTS if !BT
     select RING_BUFFER
     help
         Enables Enhanced ShockBurst protocol for ZMK split keyboards.
--- a/src/split/esb/Kconfig
+++ b/src/split/esb/Kconfig
@@ -139,7 +139,7 @@
 
 config ZMK_SPLIT_ESB_USE_TIMESLOT
     bool "Use MPSL timeslot to schedule radio"
-    depends on BLE
+    depends on BT && BT_LL_SOFTDEVICE
     select MPSL_ASSERT_HANDLER
     help
         When enabled, ESB has interrupted radio access with MPSL
